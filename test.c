#include <sys/types.h>
#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <unistd.h>
#include <sys/wait.h>

/*
 * Outputs an array of structs ready to be parsed into exec()
 *         as well as the number of arguments into num_cmd
 */
struct exec_info* parse_input(int argc,
                              char** argv,
                              size_t maxstrlen,
                              size_t* num_cmd) {
    
}

int main(int argc, char** argv) {
    
}
